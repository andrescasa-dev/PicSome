import { createContext } from 'react'
const userContext = createContext()

// TODO: create a custom component, that works like a wrapper in order to do composition... it sould be called "ContextProvider"

export default userContext
